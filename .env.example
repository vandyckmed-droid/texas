# Financial Modeling Prep API key, used only by `npm run refresh` (never bundled into the app).
# In the Claude Code environment the key is provided as API_KEY; either name works.
API_KEY=your-fmp-api-key
